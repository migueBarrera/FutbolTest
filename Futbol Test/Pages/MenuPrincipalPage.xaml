<Page
    x:Class="Futbol_Test.Pages.MenuPrincipalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Futbol_Test.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Primera Fila-->
        <Grid>
            <AppBarButton Icon="More" FontSize="15" Click="AppBarButton_Click" HorizontalAlignment="Right"/>
        </Grid>

        <Image Source="../Assets/SplashScreen.scale-150.png" Stretch="None"/>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center"  VerticalAlignment="Center" >
            <Button  Content="Modo Test por Reglas" Click="Button_Click_1"/>
        </StackPanel>

        <StackPanel Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <Button  Content="Modo Examen" Click="Button_Click"/>
        </StackPanel>


    </Grid>

</Page>
